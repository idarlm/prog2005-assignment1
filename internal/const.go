package endpoints

// Server properties

const DEFAULT_PORT = "8080"

// Version root paths

const ROOT_PATH_V1 = "/unisearcher/v1"

// Endpoint branch paths

const (
	UNIINFO_PATH      = "/uniinfo/"       // Path component for uniinfo endpoint
	NEIGHBOURUNI_PATH = "/neighbourunis/" // Path component for neighbourunis endpoint
	DIAG_PATH         = "/diag/"          // Path component for diag endpoint
)

const COFFEE_PATH = "/coffee" // EXTREMELY IMPORTANT!
